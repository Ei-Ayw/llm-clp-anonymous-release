# Checkpoints

Place optional LLM-CLP `.pth` checkpoints here or under `outputs/models/`.

The anonymous source package does not include trained weights by default. If checkpoints are added for review, ensure that checkpoint metadata does not reveal author names, local usernames, institutional paths, or private machine names.

